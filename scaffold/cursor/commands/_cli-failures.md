# CLI and tool failures

**Canonical doc (read in full):** [../skills/ai-spector/references/cli-failures.md](../skills/ai-spector/references/cli-failures.md)

Slash-command agents must follow that file: pause on failure, report output, offer **fix and retry** vs **approved workaround** vs **pause** — do not silently bypass CLI or block the user without options.
