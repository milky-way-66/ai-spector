[project]
name = "ai-spector-cocoindex"
version = "0.1.0"
requires-python = ">=3.11"
dependencies = [
    "cocoindex>=0.3.39,<1.0",
    "mcp>=1.0",
    "sentence-transformers>=3.0",
    "lancedb>=0.8",
]

[project.optional-dependencies]
openai = ["openai>=1.0"]
postgres = ["psycopg2-binary>=2.9"]
